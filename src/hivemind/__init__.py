"""Hivemind — a shared memory service for an organization's AI agents.

One organization, one pool. Agents read and write distilled entries
(facts, insights, decisions) with full provenance; hybrid search
(keyword + vector, RRF-fused, decay-aware) makes one agent's work
discoverable to every other agent in the organization.
"""

__version__ = "1.0.0rc3"
