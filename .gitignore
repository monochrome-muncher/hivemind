# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.venv/
venv/

# Tooling
.mypy_cache/
.ruff_cache/
.pytest_cache/

# Local data
# Environment profile files (ADR 0017): the tracked master template is
# config/.env.example; the per-environment copies are operator data.
.env
.env.local
.env.production
.env.staging
.env.test
*.db
*.sqlite3

# Docker volumes (named volumes live outside the repo; ignore mount points)
data/

# OS
.DS_Store